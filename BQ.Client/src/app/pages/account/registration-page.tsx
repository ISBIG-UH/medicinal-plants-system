import React from 'react';
import LeafBackground from '../../app-leaf-background';
import { Registration } from '../../../features/account';

const RegistrationPage: React.FC = () => {
    return (
        <LeafBackground>
            <Registration />
        </LeafBackground>
    );
};

export default RegistrationPage;
